<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="repository.mapper.ArticleMapper">
	<insert id="insertArticle" parameterType="int">
		INSERT INTO ARTICLE(ID,
		WRITE_TIME, CONTENT, PHOTO_PATH)
		VALUES(#{id}, #{write_time}, #{content}, #{photo_path})
	</insert>

	<select id="selectAllArticle" resultType="ArticleVO">
		select
		*
		from
		article
		where id in (select 
						followee 
					from 
						follow 
					where id = #{id}) OR id = #{id}
		ORDER BY WRITE_TIME DESC;
	</select>
</mapper>